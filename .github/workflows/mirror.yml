name: 'Mirror Repo'
on:
  push:
    branches: [__mirror]
  schedule:
    - cron: '0 * * * *'   # Run 
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: bridgelightcloud/github-mirror-action@v3
        with:
          origin: 'https://github.com/rkeyoceannetworks/cloudloop-mqtt'
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
